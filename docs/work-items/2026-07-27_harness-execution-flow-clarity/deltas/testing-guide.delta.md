# Testing Guide Delta — Execution Flow Clarity

Work ID: `2026-07-27_harness-execution-flow-clarity`

Validate execution routing with `python .agents/skills/dev-doc-harness/scripts/test_harness_policy.py`. The validator protects the ordered method cascade, mandatory route-specific review, the native no-review stop, fresh method-only and model-only overrides, and the grouped next-stage summary. The summary distinguishes current planning facts from the draft recommendation or frozen approval, validates Method/Run in/Plan Task reviewers and Model/Reasoning, rejects context speculation, and checks the matching draft-review, freeze, and handoff chat projection. A same-Codex-task fixture must show a known-suitable profile, suitable or immaterial context risk, and a concrete continuity benefit; draft and frozen state labels are mutually exclusive. It also rejects source blocks and generated plan templates that render both next-stage headings together. The combined-package fixture accepts a normal small/medium spec-and-plan pair, rejects an unauthorized lone spec, permits a lone spec only with an operator-requested or operator-approved staged reason and `plan drafting`, retains the large/phased anchor, and keeps one-thread-manageable complexity small/medium.
