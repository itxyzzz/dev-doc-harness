# Testing Guide Delta — Execution Flow Clarity

Work ID: `2026-07-27_harness-execution-flow-clarity`

Validate execution routing with `python .agents/skills/dev-doc-harness/scripts/test_harness_policy.py`. The validator protects the ordered method cascade, route-specific review, independently reviewed native execution, a pending no-review operator decision, operator-authorized no-review execution, an operator-declined review with recorded authorization, and rejection of silent no-review execution. It also protects fresh method-only and model-only overrides and the grouped next-stage summary. The summary distinguishes current planning facts from the draft recommendation or frozen approval, validates Method/Run in/Plan Task reviewers and Model/Reasoning, rejects context speculation, and checks the matching draft-review, freeze, and handoff chat projection. A same-Codex-task fixture must show a known-suitable profile, suitable or immaterial context risk, and a concrete continuity benefit; draft and frozen state labels are mutually exclusive. It also rejects source blocks and generated plan templates that render both next-stage headings together. The combined-package fixture accepts a normal small/medium spec-and-plan pair, rejects an unauthorized lone spec, permits a lone spec only with an operator-requested or operator-approved staged reason and `plan drafting`, retains the large/phased anchor, and keeps one-thread-manageable complexity small/medium.

Amendment 004 makes the same validator require exactly one current-planning field as plan or phase-header metadata, a single four-group draft summary in the final handoff or transition, and an assessment-only shared Model and Sub-agent Strategy block. It checks that the generated small/medium and phase plans preserve that placement and group order, while the large anchor retains its single existing strategy presentation. It also verifies that routine small/medium router and source-header guidance follows `module:quality` and loads `module:artifact-style` only when the artifact becomes large or hard to scan; large anchors remain mandatory and phase plans remain conditional.

Amendment 002 adds semantic bootstrap coverage for activation, sizing, model policy, Superpowers ownership, canonical artifact location, combined planning, guarded legacy compatibility, execution start, operator overrides, and distribution scope, plus a 360-word ceiling for root `AGENTS.md`. `PASS skill.openai-metadata` requires the minimal three-field `agents/openai.yaml`, validates the approved values and short-description length, requires `$dev-doc-harness` in the default prompt, and rejects extra interface fields without adding a YAML dependency. Also run the installed skill creator's `quick_validate.py` against `.agents/skills/dev-doc-harness`.

Amendment 003 makes `PASS execution.thread-start` verify the execution-quality consumer boundary. The startup protocol must cite `rule:freeze.stop-before-implementation`, `rule:models.selection-dimensions`, and `rule:models.execution-continuity`; load applicable instructions and the approved frozen inputs; verify the worktree and validation baseline; avoid broad rediscovery; begin the named activity; and route variance to its lifecycle owner. Transition, override, uninterrupted-execution, and chat-projection assertions now read from `planning-freeze-gates.md`, while `presentation.next-stage-summary` keeps its existing behavior and template fixtures without requiring a second presentation owner in `context-and-quality-gates.md`.

The post-review transition checks require the four-group definition under `Draft review checkpoint`, a reference rather than a duplicate definition at approval freeze, and exactly two execution-continuity branches. For `new Codex task`, compatible agent task creation after specific approval is the default continuation; manual operator creation is limited to unavailable or incompatible task creation or an explicit operator request. The checks also require runtime overrides in the completion report, conditional variance-log use, and multiple freeze gates as the normal large/phased flow.

Section-ownership checks keep freeze, same-turn stop, and routing-pointer mechanics in `Approval freeze checkpoint`, while `Post-freeze transition routing` owns authorization, runtime overrides, and the effective execution handoff. The effective values start from the frozen **Approved next stage**, apply any explicit operator override, and preserve the frozen artifact rather than rewriting it.
