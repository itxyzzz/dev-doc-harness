# Spec Template Alignment Plan

Work ID: `2026-08-03_spec-template-alignment`
Short ID: `spec-template-alignment`
Status: Approved
Harness release: `0.8+`
Schema: `schema:plan.small-medium`
Policy references: `module:lifecycle`, `module:naming`, `module:quality`, `module:models`, `module:architecture`, `rule:models.strategy-required`, `rule:models.context-strategy`, `rule:models.approved-strategy-authorized`, `rule:models.fresh-confirmation`, `rule:lifecycle.commit-message-format`, `rule:lifecycle.variance-policy`, `rule:naming.derived-patterns`, `rule:naming.work-item-paths`, `rule:naming.commit-messages`

## Input Artifacts

1. Draft spec: `spec_spec-template-alignment.md`.
2. Architecture input: Architecture decisions in the draft spec; no architecture snapshot is required because the established block/manifest/assembler design is retained.
3. Required snapshots or deltas: None.
4. Relevant repository inputs: `.agents/skills/dev-doc-harness/SKILL.md`; `references/artifact-contract.md`, `durable-planning-quality.md`, `maintenance-architecture.md`, `naming-conventions.md`, and `subagent-model-policy.md`; the two spec manifests and all blocks named in the Change surfaces section; `scripts/assemble_templates.py`; and `scripts/test_harness_policy.py`.
5. Unresolved implementation context to confirm before editing: None. The operator approved the exact small and large template changes in the current task.

## Traceability approach

Use local `SPEC-*` and `VER-*` references in each task. A full mapping is unnecessary because five bounded tasks cover all commitments without an ambiguous many-to-many relationship.

## Change surfaces

1. `.agents/skills/dev-doc-harness/assets/templates/blocks/spec.020.common.intent-scope-context.md`: relocate one Assumptions and Open Questions section after Repository Context, with two subsections.
2. `.agents/skills/dev-doc-harness/assets/templates/blocks/spec.040.common.interfaces-risks.md`: rename and compact the shared impact section while preserving the agreed four subsections.
3. `.agents/skills/dev-doc-harness/assets/templates/blocks/spec.085.small.handoff.md`: retain default combined planning; compress only the staged spec-only exception.
4. `.agents/skills/dev-doc-harness/assets/templates/blocks/spec.090.small.readiness-approval.md`: install the agreed six-check checklist.
5. `.agents/skills/dev-doc-harness/assets/templates/blocks/spec.010.large.header.md`: defer freeze-gate policy references from draft-time metadata.
6. `.agents/skills/dev-doc-harness/assets/templates/blocks/spec.050.large.operations.md`: promote operations to a top-level section.
7. `.agents/skills/dev-doc-harness/assets/templates/blocks/spec.070.large.planned-commits-freeze.md`: remove duplicate rolling/batch prose and make freeze-gate loading conditional on review or approval.
8. `.agents/skills/dev-doc-harness/assets/templates/blocks/spec.085.large.handoff.md`: clarify anchor versus later phase-plan transition ownership.
9. `.agents/skills/dev-doc-harness/assets/templates/blocks/spec.090.large.readiness-approval.md`: make the checklist concise while preserving all large-specific checks and the revised Impact Surfaces terminology.
10. `.agents/skills/dev-doc-harness/scripts/test_harness_policy.py`: replace staged full-summary assertions with compact staged-exception assertions and adjust wording-sensitive expectations without weakening large-anchor coverage.
11. `.agents/skills/dev-doc-harness/assets/templates/small-medium-work-item-spec.md` and `large-phased-work-item-spec.md`: generated by the assembler; do not edit directly.

## Implementation approach

Edit the authoritative blocks first. Preserve the existing manifests and their block order, because section reordering occurs inside the shared context block and the large operations block stays in its current manifest position while becoming a top-level section. Update the validator to distinguish the compact staged exception from the full large-anchor next-stage contract. Then regenerate the two flat templates through the assembler and validate the complete current harness surface.

## Model and Sub-agent Strategy

Upcoming-stage sub-agent assessment:

1. Sub-agents: None.
2. Fit reason: The source blocks, generated outputs, and structural validator form one tightly coupled contract; a single orchestration session can preserve their exact wording and run deterministic validation with lower coordination cost than a delegated worker.
3. Authorization state: Not needed.

## Implementation tasks

### `TASK-001` Reorder shared context and impact-surface blocks

Dependencies: Approved planning package and fresh execution authorization.

Interfaces:

1. Consumes: `SPEC-001`, `VER-001`, `spec.020.common.intent-scope-context.md`, and `spec.040.common.interfaces-risks.md`.
2. Produces: Revised shared block text consumed by the small and large spec assembly manifests.

Implementation:

1. In `spec.020.common.intent-scope-context.md`, leave Goal, Source and Intent, Scope Boundary, and the three Repository Context subsections intact.
2. Remove `### Assumptions` and `### Open questions` from Scope Boundary.
3. Add `## Assumptions and Open Questions` immediately after `### Constraints and compatibility`.
4. Add `### Assumptions` under that section, retaining the existing assumptions prompts, then add `### Open questions` with the existing ownership and no-open-question prompts.
5. In `spec.040.common.interfaces-risks.md`, replace the current impact section with exactly this compact shape:

```md
## Impact Surfaces

Record any impacted surfaces below. Use bullets for lists; state `None` when the respective surfaces are not affected or are not relevant.

### Interfaces

Record public APIs, internal interfaces, CLI flags, config, schemas, generated artifacts, templates, or docs affected by the change.

### Data, config, and persistence

Record data model, persistence, migration, configuration, release-identity, or rollout effects.

### State and control flow

Record lifecycle, routing, state-machine, validation, request flow, jobs, concurrency, retries, or process-flow changes.

### Safety, security, privacy, migration, and rollback

Record safety, auth, data exposure, privacy, compliance, migration, rollout, rollback, destructive-operation, and operator-safety considerations.
```

6. Leave the Risks and Rejected Alternatives section and all four risk prompts unchanged.

Exit criteria: The shared blocks contain the agreed section structure and wording without copying small- or large-only lifecycle behavior.

#### `CHECK-001` Verify shared source structure

Covers: `VER-001`.

Method: Run `rg -n "^## (Repository Context|Assumptions and Open Questions|Impact Surfaces)$|^### (Constraints and compatibility|Assumptions|Open questions|Interfaces|Data, config, and persistence|State and control flow|Safety, security, privacy, migration, and rollback)$" .agents/skills/dev-doc-harness/assets/templates/blocks/spec.020.common.intent-scope-context.md .agents/skills/dev-doc-harness/assets/templates/blocks/spec.040.common.interfaces-risks.md`.

Expected result: The context block lists Constraints and compatibility before one top-level Assumptions and Open Questions heading with its two subsections; the impact block matches the approved compact prompt and four agreed subsections.

Evidence record: Implementation completion report with the command output summary.

### `TASK-002` Apply the small-spec handoff and checklist decisions

Dependencies: `TASK-001`.

Interfaces:

1. Consumes: `SPEC-002`, `VER-002`, `spec.085.small.handoff.md`, and `spec.090.small.readiness-approval.md`.
2. Produces: A default combined package unchanged in lifecycle effect, a compact staged exception, and the six approved readiness checks.

Implementation:

1. Retain the Default combined package list in `spec.085.small.handoff.md`, including its companion plan, transition owner, and `plan execution` next stage.
2. Replace the staged exception prose and its nested `### Next-stage recommendation` block with a concise staged-exception statement that requires the approved staging reason, the spec-only frozen package, and `Next lifecycle stage: plan drafting`.
3. Do not add a model, orchestration, or sub-agent selection block to the staged exception.
4. Replace the checklist in `spec.090.small.readiness-approval.md` with these six approved checks:

```md
- [ ] Goal, source and intent, scope, constraints, architecture decisions, commitment statements, and verifications are mutually consistent.
- [ ] All relevant operator input is preserved in this specification or through `module:evidence` and `rule:evidence.preservation`.
- [ ] Commitment statements are atomic, bounded, and form a complete set that covers the full scope and achieves the goal; no obligation exists only in rationale or examples.
- [ ] Verification criteria form a complete set that covers all Commitments and have no hidden procedure or scope.
- [ ] This specification file with `snapshots/architecture.snapshot.md` is self-contained so a fresh session can draft the actionable plan without reconstructing original session context.
- [ ] No unresolved placeholders, plan-affecting decisions, missing sections, or ownerless deferrals remain.
```
5. Retain the Approval block unchanged.

Exit criteria: The small template still directs default combined work to the companion plan, while staged planning no longer duplicates the full next-stage recommendation.

#### `CHECK-002` Verify small default and staged paths

Covers: `VER-002`.

Method: Run `rg -n -C 2 "Default combined package|Transition owner|Next lifecycle stage: `plan execution`|explicit staged|spec-only frozen package|Next lifecycle stage: `plan drafting`|Next-stage recommendation" .agents/skills/dev-doc-harness/assets/templates/blocks/spec.085.small.handoff.md`.

Expected result: Default combined-package transition facts remain; the staged exception names only its reason, frozen package, and `plan drafting`; no staged nested recommendation remains.

Evidence record: Implementation completion report with the command output summary.

### `TASK-003` Apply conservative large-anchor cleanup

Dependencies: `TASK-001`.

Interfaces:

1. Consumes: `SPEC-003`, `VER-003`, and large blocks `spec.010`, `spec.050`, `spec.060`, `spec.070`, `spec.085`, and `spec.090`.
2. Produces: A large anchor that preserves its phase-planning and model contract while eliminating identified flow, duplication, and draft-route inconsistencies.

Implementation:

1. Remove `module:freeze-gate` and `rule:freeze.multi-gate-flow` from `spec.010.large.header.md` Policy references, leaving lifecycle, model, style, naming, and quality references intact.
2. Change the heading in `spec.050.large.operations.md` from `### Triage, debugging, and operations` to `## Triage, debugging, and operations`; retain both operational prompts.
3. Remove the rolling/batch sequence paragraph from the Planned commits portion of `spec.070.large.planned-commits-freeze.md`.
4. Change that block’s freeze section opening to load `module:freeze-gate` and its named rules only at draft review or approval; retain the anchor-spec-only and no-early-phase-plan requirements.
5. In `spec.085.large.handoff.md`, remove `rule:execution-quality.execution-thread-start` from the anchor-transition reference line.
6. Replace its final anchor-to-phase bullet with wording that assigns `phase-plan drafting` to the anchor’s approved next-stage selection and phase-execution handoff, artifacts, and variance stop conditions to each later phase plan at its own freeze boundary.
7. Do not alter `spec.060.large.phase-decomposition-model.md`; its large-anchor Next-stage recommendation and phase-decomposition/model content remain required.
8. Replace the large readiness checklist with exactly these concise checks, preserving every large-specific concern:

```md
- [ ] Goal, source and intent, scope, constraints, architecture decisions, commitment statements, and verifications are mutually consistent.
- [ ] All relevant operator input is preserved in this specification or through `module:evidence` and `rule:evidence.preservation`.
- [ ] Commitment statements are atomic, bounded, and form a complete set that covers the full scope and achieves the goal; no obligation exists only in rationale or examples.
- [ ] Verification Criteria cover all applicable Commitments, have no hidden procedure or scope, and identify cross-phase ownership where applicable.
- [ ] This specification file with `snapshots/architecture.snapshot.md` is self-contained enough that a fresh session can draft each actionable phase plan without reconstructing original session context.
- [ ] Phase decomposition explains why each phase belongs, identifies its future phase-plan output, and keeps each phase within one orchestration session or records the escalation boundary.
- [ ] The Next-stage recommendation records the required lifecycle, orchestration, model, fallback, and stage-specific sub-agent decisions; optional current-session diagnostics are omitted unless material, and the recommendation is relabeled `Approved next stage` at freeze.
- [ ] Impact Surfaces, triage/debugging/operations, risks, documentation obligations, planned commits, and any batch-planning exception have been assessed.
- [ ] No unresolved placeholders, plan-affecting decisions, missing sections, or ownerless deferrals remain.
```

Exit criteria: The large anchor keeps its unique phase-planning and model content, has an unambiguous operations section, and states rolling/batch and phase-handoff semantics once at their correct owner.

#### `CHECK-003` Verify large-anchor distinctness and flow

Covers: `VER-003`.

Method: Run `rg -n "^## (Triage, debugging, and operations|Phase decomposition|Model and Sub-agent Strategy|Anchor-to-phase transition)$|^### Next-stage recommendation$|rolling|stable.*independent|At draft review or approval|phase-execution handoff" .agents/skills/dev-doc-harness/assets/templates/blocks/spec.010.large.header.md .agents/skills/dev-doc-harness/assets/templates/blocks/spec.050.large.operations.md .agents/skills/dev-doc-harness/assets/templates/blocks/spec.060.large.phase-decomposition-model.md .agents/skills/dev-doc-harness/assets/templates/blocks/spec.070.large.planned-commits-freeze.md .agents/skills/dev-doc-harness/assets/templates/blocks/spec.085.large.handoff.md .agents/skills/dev-doc-harness/assets/templates/blocks/spec.090.large.readiness-approval.md`.

Expected result: Operations is top-level; the large Next-stage recommendation remains; rolling/batch guidance is emitted by Anchor-to-phase transition rather than Planned commits; and phase-execution handoff is attributed to later phase plans.

Evidence record: Implementation completion report with the command output summary.

### `TASK-004` Update structural validator expectations

Dependencies: `TASK-002`, `TASK-003`.

Interfaces:

1. Consumes: `SPEC-004`, `VER-004`, revised block contracts, and `scripts/test_harness_policy.py` staged/large next-stage checks.
2. Produces: Targeted structural checks that distinguish a compact staged exception from the full large-anchor summary while retaining unrelated protection.

Implementation:

1. In the governed-transition-template list, remove `spec.085.small.handoff.md` as a template required to satisfy `next_stage_template_contract_errors`.
2. Add positive staged-source assertions for explicit staged approval/reason, spec-only frozen package, and `plan drafting` next lifecycle stage.
3. Add a negative staged-source assertion that rejects `### Next-stage recommendation` within the staged small handoff source.
4. Retain the existing small generated-template assertion that protects `Transition owner.*plan` and the existing large-anchor full-summary checks sourced from `spec.060.large.phase-decomposition-model.md`.
5. Add focused readiness assertions for the operator-input-preservation check in the small and large readiness source blocks and their generated spec templates.
6. Update wording-sensitive assertions only as needed for `Impact Surfaces`, the top-level operations heading, deferred freeze-gate phrase, and the revised anchor-to-phase handoff wording.
7. Do not reduce the assembly freshness check, schema checks, phase-plan handoff checks, rolling/batch assertions, or large-anchor next-stage validation.

Exit criteria: The validator encodes the approved distinction between small staged planning and the large anchor without weakening current unrelated checks.

#### `CHECK-004` Run full harness validation

Covers: `VER-004`, `VER-005`.

Method: Run `python .agents/skills/dev-doc-harness/scripts/test_harness_policy.py`.

Expected result: The command exits 0 and reports no failure from staged-summary, large-anchor, generated-template, or assembly-freshness validation.

Evidence record: Full command output in the implementation completion report.

### `TASK-005` Assemble, inspect, and record implementation evidence

Dependencies: `TASK-001`, `TASK-002`, `TASK-003`, `TASK-004`.

Interfaces:

1. Consumes: Revised source blocks, unchanged manifests, revised validator, and `VER-001` through `VER-005`.
2. Produces: Regenerated current spec templates and recorded validation evidence.

Implementation:

1. Run `python .agents/skills/dev-doc-harness/scripts/assemble_templates.py --write` to regenerate the two affected published spec templates and run validation.
2. Review the generated diff to confirm that only `small-medium-work-item-spec.md` and `large-phased-work-item-spec.md` change among published templates.
3. Run the freshness check after regeneration.
4. Run whitespace validation and inspect the final worktree for unrelated changes before staging.
5. Create `changelog/implementation-fragment.md` after execution starts and update the root `CHANGELOG.md` before the implementation commit, using the approved implementation subject unless a recorded variance changes it.

Exit criteria: Generated templates are fresh, validation is clean, and implementation evidence plus changelog obligations are ready for the cohesive implementation commit.

#### `CHECK-005` Verify generated outputs and clean diff

Covers: `VER-001`, `VER-002`, `VER-003`, `VER-005`.

Method: Run these commands in order:

1. `python .agents/skills/dev-doc-harness/scripts/assemble_templates.py --check`
2. `git diff --check`
3. `git diff -- .agents/skills/dev-doc-harness/assets/templates/small-medium-work-item-spec.md .agents/skills/dev-doc-harness/assets/templates/large-phased-work-item-spec.md`
4. `git status --short`

Expected result: Freshness check exits 0; whitespace check emits no errors; generated-template diff contains the agreed source-derived structural changes; worktree contains only planned implementation targets, the implementation changelog fragment, and root changelog update.

Evidence record: Implementation completion report with command summaries and final changed-file list.

## Planned commits

| Stage | Planned subject |
|---|---|
| Planning approval | `plan: spec-template-alignment -- approve spec template cleanup` |
| Implementation | `docs: spec-template-alignment -- align assembled spec templates` |

One cohesive implementation commit is the default because template blocks, their generated outputs, structural validation, and the implementation changelog must remain synchronized.

## Validation and variance

Plan Checks `CHECK-001` through `CHECK-005` produce the required evidence. Before freeze, update this draft directly for operator feedback. After freeze, record noteworthy implementation drift in `implementation-notes/variance-log.md`; use an amendment for material scope, lifecycle, schema, model-policy, or validation-contract changes.

Allowed minor variance:

1. Adjust exact validator assertion patterns if source-block wording differs while preserving every stated contract and rejection condition.
2. Make small Markdown line-wrap changes in the two generated templates that result solely from the approved block edits.

Operator approval required before continuing:

1. Changing the documentation artifact matrix or `artifact-contract.md`.
2. Removing, moving, or materially changing the large anchor’s Next-stage recommendation, phase decomposition, Model and Sub-agent Strategy, or rolling/batch lifecycle semantics.
3. Changing any published template path, schema identifier, assembly manifest block order, lifecycle-stage value, or model-policy rule.
4. Weakening assembly-freshness or unrelated current validator checks.

## Implementation handoff

### Approved next stage

#### Next lifecycle stage

Stage: `plan execution`.

#### Orchestration

Method: `superpowers:executing-plans`; Orchestration mode: `single-agent`; Run in: `same orchestration session`; Review: preserve execution checkpoints and disclose reviewer capability at execution start; no independent reviewer is planned because the change is tightly coupled and low blast radius, but a later operator-approved reviewer may be added within the recorded variance boundary.

#### Model

Generation: `latest available`; Capability tier: `balanced`; Reasoning: `medium`.

#### Fallbacks and limits

Use only the frozen spec, plan, referenced blocks, and validation outputs; stop for a material variance listed above. Fresh explicit operator authorization is required after planning freeze before any implementation edit.

1. Frozen package: approved `spec_spec-template-alignment.md` and `plan_spec-template-alignment.md`.
2. Artifact rehydration: Read the frozen package, the affected source blocks, the two manifests, `assemble_templates.py`, and focused validator assertions before editing.
3. Variance stop condition: Stop for the approval-required changes listed in Validation and variance.

## Readiness

- [x] Inputs, scope, tasks, checks, documentation, and changelog obligations are explicit.
- [x] The default small path and the distinct large-anchor path have separate, non-conflicting task coverage.
- [x] The grouped next-stage selection, implementation handoff, and upcoming-stage sub-agent assessment are explicit.
- [x] No required decision or ownerless deferral remains.

## Completion

- Required work and evidence are complete; any noteworthy variance is recorded.
- Planned changes are committed, or the blocker is stated.

## Approval

- Status: Approved
- Superseded by: None
