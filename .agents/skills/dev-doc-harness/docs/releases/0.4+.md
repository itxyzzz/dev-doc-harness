# Dev Doc Harness 0.4+

## Release

Dev Doc Harness `0.4+` adds source-block assembly for the primary planning templates and hardens durable artifact readability while keeping the published templates self-contained.

## Package Contents

The distributable package is root `AGENTS.md` plus `.agents/`.

It does not copy this repository's root `README.md`, root `CHANGELOG.md`, root `TODO.md`, `docs/work-items/`, `.git/`, `.githooks/`, or other local development files.

## Added

- Added ordered template source blocks under `assets/templates/blocks/`.
- Added explicit assembly manifests under `assets/templates/assemblies/`.
- Added `scripts/assemble_templates.py` with `--list`, `--check`, and `--write`.
- Added validation coverage for generated-template freshness and block or manifest structure.
- Added `references/artifact-style.md` for final artifact content, scannable structure, placeholder control, traceability density, and template prompt style.
- Added validation coverage for artifact-style ownership, large-anchor routing, final-state template cues, canonical model-policy examples, and decision/amendment/variance IDs.

## Changed

- Changed the small/medium and large/phased spec and plan templates to be generated flat Markdown files.
- Changed current requirement, acceptance-criterion, and risk examples to use `###` headings with tag IDs.
- Changed the package-local release marker from `0.3.0` to `0.4+`.
- Changed large anchor spec routing so `module:artifact-style` is required while routine small/medium routes keep only baseline readability guidance unless artifact readability risk is material.
- Changed planning, architecture snapshot, amendment, and variance templates to use stronger final-state fields, unresolved-decision readiness checks, model-policy source prompts, and evidence cross-references.

## Compatibility

Agents and downstream adopters still consume complete Markdown templates from `assets/templates/`.

Maintainers edit source blocks and manifests, then run the assembler before committing template changes.

## Team Adoption

Copy root `AGENTS.md` and `.agents/` into the target repository, merge local `AGENTS.md` instructions carefully, run validation when practical, and commit the harness update separately from product work.

The optional `.githooks/` files in this repository are not part of the distributable package.

## Rollback

Revert the dedicated harness update commit or PR to restore the previous root `AGENTS.md` and `.agents/` package.

## Source Changelog Entries

- `2026-07-03_artifact-style-guidance -- add artifact style module`
- `2026-07-02_template-block-assembly -- generate 0.4+ templates from shared blocks`
- `2026-07-02_orchestration-sizing-large-templates -- align sizing and phased templates`
- `2026-07-01_small-medium-plan-template-structure -- label task dependencies in plan template`
- `2026-07-01_small-medium-plan-template-structure -- improve plan-template scaffolding`
- `2026-07-01_small-medium-template-structure -- improve spec-template scaffolding`
