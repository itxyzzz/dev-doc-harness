## Implementation tasks

### `TASK-001` `<bounded outcome>`

Dependencies: `<None or TASK-NNN>`.

Implementation:

1. `<executable change>`.

Exit criteria: `<observable completed result>`.

#### `CHECK-001` `<evidence title>`

Covers: `VER-001`.

Method: `<command, inspection, or review>`.

Expected result: `<observable success signal>`.

Evidence record: `<path, command output, or review note>`.
